# HYDRA PATCH APPLICATION PREVIEW GATE MANIFEST v001

Package: `hydra_patch_application_preview_gate_v001_READY.zip`

Purpose: create a metadata-only approval/preview packet for a Codex return that has already passed return intake and return review as `PATCH_PROPOSED_APPLICATION_GATE_REQUIRED`.

## Files

- `HYDRA_PATCH_APPLICATION_PREVIEW_GATE_MANIFEST_v001.md`
- `run_hydra_patch_preview_gate_v001.ps1`
- `hydra_patch_preview_gate_v001.py`
- `hydra_patch_preview_policy_v001.json`

## Boundaries

This package does not apply patches, edit source files, execute returned code, run tests, train models, score, predict, promote, reopen P1, execute backups, join sources, expand datasets, engineer features, attach targets/labels, do gamma work, parse content, reopen ML slices, reopen the non-ML drain, or re-enter stale selectors.

## Future apply phrase

Patch application remains locked behind a separate future gate requiring this exact phrase:

`AUTHORIZE_HYDRA_CODEX_PATCH_APPLICATION_V001`

Do not use that phrase unless patch application is actually intended.
