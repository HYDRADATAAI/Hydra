name: Constraint policy integration

on:
  pull_request:
    paths:
      - "constraint-geopolitical-policy/**"
      - "constraint-physical-dependency/**"
      - ".github/workflows/constraint-policy-integration.yml"
  workflow_dispatch:

jobs:
  policy-physical-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install stacked Constraint packages
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ./constraint-physical-dependency -e ./constraint-geopolitical-policy
      - name: Compile packages
        run: |
          python -m compileall -q constraint-physical-dependency/src constraint-geopolitical-policy/src
      - name: Test physical foundation
        run: python -m unittest discover -s constraint-physical-dependency/tests -p "test_*.py" -v
      - name: Test policy and cross-layer integration
        run: python -m unittest discover -s constraint-geopolitical-policy/tests -p "test_*.py" -v
