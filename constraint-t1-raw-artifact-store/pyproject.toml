[project]
name = "hydra-constraint-t1-raw-artifact-store"
version = "0.2.0"
description = "Private immutable raw-artifact persistence for HYDRA Constraint T1"
requires-python = ">=3.11"
dependencies = []

[tool.hydra]
owner = "PIPELINE_T1_SOURCE_ACQUISITION"
network-acquisition = false
public-raw-content = false
immutable-artifacts = true
