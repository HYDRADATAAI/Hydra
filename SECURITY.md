# Security

Do not commit credentials, API keys, access tokens, private keys, private datasets, or machine-local configuration.

This public repository is staged from a larger private research tree through a deny-list and safety scan.

If sensitive information is discovered, revoke or rotate it first and report only the affected path, never the secret value.
